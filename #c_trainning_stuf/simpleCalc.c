#include <stdio.h>
int main()
{
    int a = 5;
    int b = 10;

    printf("%i + %i = %i\n", a, b, a + b);
    printf("%i - %i = %i\n", a, b, a - b);
    printf("%i x %i = %i\n", a, b, a * b);
    printf("%i / %i = %i\n", a, b, a / b);
    return 0;
}